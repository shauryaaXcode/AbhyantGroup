import { ArrowUpRight, MapPin, Ruler, CheckCircle2 } from 'lucide-react'; import { Link } from 'react-router-dom'; import { motion } from 'framer-motion'; import SectionIntro from './SectionIntro'; import { projects } from '../data/projects';
export function ProjectCard({project}) {return <motion.article whileHover={{y:-7}} className="group overflow-hidden rounded-2xl border border-slate-100 bg-white shadow-card"><div className="relative h-56 overflow-hidden"><img loading="lazy" src={project.image} alt={`${project.name} project`} className="h-full w-full object-cover transition duration-500 group-hover:scale-105"/><span className="absolute left-4 top-4 rounded-full bg-white px-3 py-1 text-[10px] font-extrabold uppercase tracking-wide text-ink">{project.status}</span></div><div className="p-5"><h3 className="font-display text-2xl text-ink">{project.name}</h3><p className="mt-2 flex items-center gap-2 text-sm text-slate-500"><MapPin size={15} className="text-gold"/>{project.location}</p><div className="mt-5 grid grid-cols-2 gap-3 border-y border-slate-100 py-4 text-sm"><span className="text-slate-500"><Ruler size={14} className="mr-1 inline text-moss"/>{project.size}</span><span className="font-bold text-ink">From {project.price}</span></div><div className="mt-4 flex items-center justify-between"><span className="flex items-center gap-1 text-xs text-moss"><CheckCircle2 size={14}/>{project.availability}</span><Link to={`/projects/${project.id}`} className="flex items-center gap-1 text-sm font-bold text-ink transition hover:text-gold">View Details <ArrowUpRight size={15}/></Link></div></div></motion.article>}
export default function Projects(){return <section id="projects" className="section-space page-shell"><div className="flex flex-wrap items-end justify-between gap-6"><SectionIntro eyebrow="Find your place" title="Our Featured Projects" text="Explore our premium plotting projects in prime locations."/><Link to="/projects" className="link-arrow">View All Projects <ArrowUpRight size={16}/></Link></div><div className="mt-12 grid gap-6 sm:grid-cols-2 xl:grid-cols-4">{projects.map(project=><ProjectCard key={project.id} project={project}/>)}</div></section>}
