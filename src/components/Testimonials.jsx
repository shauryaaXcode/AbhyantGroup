import { useEffect,useState } from 'react'; import { ChevronLeft,ChevronRight,Star } from 'lucide-react'; import SectionIntro from './SectionIntro';
const reviews=[['“The team made our first land purchase completely effortless. The paperwork was clear and the site is beautiful.”','Aarav Verma','Homeowner'],['“Transparent pricing, genuine guidance and a smooth registry experience. Exactly what we were looking for.”','Pooja Srivastava','Investor'],['“We visited three projects; Abhyant stood out for its roads, green spaces and exceptional service.”','Rohit Mehra','Homeowner']];
export default function Testimonials(){const [idx,setIdx]=useState(0);useEffect(()=>{const t=setInterval(()=>setIdx(i=>(i+1)%reviews.length),5000);return()=>clearInterval(t)},[]);return <section className="bg-cream py-20 sm:py-28"><div className="page-shell"><SectionIntro eyebrow="Trusted by families" title="What Our Customers Say" center/><div className="relative mt-10 overflow-hidden"><div className="flex transition-transform duration-500" style={{transform:`translateX(-${idx*100}%)`}}>{reviews.map(([copy,name,role],i)=><article className="min-w-full px-1" key={name}><div className="mx-auto max-w-3xl rounded-2xl bg-white p-8 text-center shadow-card sm:p-12"><div className="mb-5 flex justify-center gap-1 text-gold">{Array.from({length:5},(_,n)=><Star fill="currentColor" size={16} key={n}/>)}</div><blockquote className="font-display text-2xl leading-relaxed text-ink sm:text-3xl">{copy}</blockquote><div className="mt-7 flex items-center justify-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-full bg-moss text-sm font-bold text-white">{name[0]}</span><p className="text-left text-sm"><b className="block text-ink">{name}</b><span className="text-slate-500">{role}</span></p></div></div></article>)}</div><button aria-label="Previous testimonial" onClick={()=>setIdx(i=>(i+reviews.length-1)%reviews.length)} className="absolute left-0 top-1/2 hidden -translate-y-1/2 rounded-full bg-white p-3 text-ink shadow sm:block"><ChevronLeft/></button><button aria-label="Next testimonial" onClick={()=>setIdx(i=>(i+1)%reviews.length)} className="absolute right-0 top-1/2 hidden -translate-y-1/2 rounded-full bg-white p-3 text-ink shadow sm:block"><ChevronRight/></button></div><div className="mt-5 flex justify-center gap-2">{reviews.map((_,i)=><button onClick={()=>setIdx(i)} className={`h-2 rounded-full transition ${idx===i?'w-6 bg-gold':'w-2 bg-slate-300'}`} aria-label={`Review ${i+1}`} key={i}/>)}</div></div></section>}
