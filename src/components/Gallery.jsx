import { useState } from 'react';import { X } from 'lucide-react';import { AnimatePresence,motion } from 'framer-motion'; import SectionIntro from './SectionIntro';import { gallery } from '../data/projects';
const categories=['All','Entrance','Roads','Parks','Aerial View','Site Office'];
export default function Gallery({limit=false}){const [active,setActive]=useState('All'),[selected,setSelected]=useState(null);const visible=(active==='All'?gallery:gallery.filter(x=>x.category===active)).slice(0,limit?3:undefined);return <section className="section-space page-shell"><SectionIntro eyebrow="See the difference" title="Project Gallery" text="Real views of our projects." center/><div className="mt-8 flex justify-center gap-2 overflow-x-auto pb-2">{categories.map(c=><button onClick={()=>setActive(c)} className={`whitespace-nowrap rounded-full px-4 py-2 text-xs font-bold ${active===c?'bg-ink text-white':'bg-cream text-slate-500'}`} key={c}>{c}</button>)}</div><motion.div layout className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{visible.map((item,i)=><motion.button layout initial={{opacity:0}} animate={{opacity:1}} onClick={()=>setSelected(item)} className={`group relative overflow-hidden rounded-2xl text-left ${i===0?'sm:col-span-2 sm:row-span-2':''}`} key={item.image}><img loading="lazy" className={`w-full object-cover transition duration-500 group-hover:scale-105 ${i===0?'h-72 sm:h-full':'h-56'}`} src={item.image} alt={item.alt}/><span className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-black/75 p-5 pt-14 text-sm font-bold text-white opacity-0 transition group-hover:opacity-100">{item.category}</span></motion.button>)}</motion.div><AnimatePresence>{selected&&<motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={()=>setSelected(null)} className="fixed inset-0 z-50 grid place-items-center bg-black/90 p-5"><button aria-label="Close gallery" className="absolute right-6 top-6 text-white"><X/></button><img onClick={e=>e.stopPropagation()} className="max-h-[85vh] max-w-full rounded-lg object-contain" src={selected.image} alt={selected.alt}/></motion.div>}</AnimatePresence></section>}
