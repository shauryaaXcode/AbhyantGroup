import { motion } from 'framer-motion';
export default function SectionIntro({eyebrow, title, text, center=false, light=false}) { return <motion.div initial={{opacity:0,y:16}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.4}} className={`${center?'mx-auto text-center':''} max-w-2xl`}><p className={`mb-3 text-xs font-bold uppercase tracking-[.2em] ${light?'text-gold':'text-moss'}`}>{eyebrow}</p><h2 className={`font-display text-4xl leading-tight sm:text-5xl ${light?'text-white':'text-ink'}`}>{title}</h2>{text&&<p className={`mt-4 leading-7 ${light?'text-white/70':'text-slate-500'}`}>{text}</p>}</motion.div> }
