import { X } from 'lucide-react';import { AnimatePresence,motion } from 'framer-motion';import InquiryForm from './InquiryForm';
export default function VisitModal({open,onClose}){return <AnimatePresence>{open&&<motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={onClose} className="fixed inset-0 z-[60] grid place-items-center bg-black/60 p-5"><motion.div initial={{opacity:0,scale:.94,y:14}} animate={{opacity:1,scale:1,y:0}} exit={{opacity:0,scale:.94,y:14}} onClick={e=>e.stopPropagation()} className="relative w-full max-w-md rounded-2xl bg-white p-7 shadow-2xl"><button onClick={onClose} className="absolute right-5 top-5 text-slate-400" aria-label="Close"><X/></button><p className="text-xs font-bold uppercase tracking-widest text-gold">Your site visit</p><h2 className="mt-2 font-display text-4xl text-ink">See it for yourself.</h2><p className="mb-6 mt-2 text-sm leading-6 text-slate-500">Tell us where you’re interested and our team will arrange a complimentary visit.</p><InquiryForm onDone={()=>setTimeout(onClose,1100)}/></motion.div></motion.div>}</AnimatePresence>}
